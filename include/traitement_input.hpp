#ifndef TRAITEMENT_INPUT_HPP
#define TRAITEMENT_INPUT_HPP

#include <vector>
#include <string>
using namespace std;

double distance(double, double, double, double);
vector<vector<double>> input_coordonnees(string);
vector<vector<double>> input_distances(string);

#endif